import { useEffect } from "react"
import { authService } from "../../service"

const Groups = () => {
  useEffect(()=> {
    
  },[])

  return (
    <div>
        <h1>Groups</h1>
    </div>
  )
}

export default Groups